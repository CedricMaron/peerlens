; Root-level pytest configuration.
; CI runs `pytest tests/ evals/` from here, where backend/pyproject.toml is
; not picked up; the async provider tests need asyncio mode either way.
[pytest]
asyncio_mode = auto
testpaths = tests evals
