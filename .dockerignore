# Never ship research data, credentials or local build artefacts.
data/
*.db
*.db-wal
*.db-shm
.env
.env.local

.git/
.github/
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

frontend/node_modules/
frontend/dist/
frontend/.vite/

tests/
evals/

# NOTE: do not exclude Markdown here. The scientific prompts in prompts/ are
# Markdown and are read at runtime — excluding them would ship a broken image.
